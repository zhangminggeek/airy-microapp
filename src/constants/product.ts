export enum ProductType {
  '婚纱' = 1,
  '礼服' = 2,
  '秀禾' = 3,
  '西服' = 4,
  '伴娘服' = 5,
  '鞋子' = 6,
  '首饰' = 7,
  '其他' = 8,
}

// 快递方式
export enum DeliveryMethod {
  '包邮' = 1,
  '到付' = 2,
  '自提' = 3,
}

// 新旧程度
export enum ProductQuality {
  '全新' = 1,
  '几乎全新' = 2,
  '轻微使用痕迹' = 3,
  '明显使用痕迹' = 4,
}
