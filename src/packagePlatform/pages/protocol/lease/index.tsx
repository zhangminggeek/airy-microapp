import { WebView } from '@tarojs/components';

const Page = () => {
  return (
    <WebView
      src="https://docs.airywedding.cn/protocol/lease"
      progressbarColor="#f76793"
    />
  );
};

export default Page;
